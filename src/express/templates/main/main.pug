extends ../layouts/main-layout

block content
  main.main-page
    h1.visually-hidden Главная страница личного блога Типотека
    p
      | Это приветственный текст, который владелец блога может выбрать, чтобы описать себя 👏
    section.main-page__theme-list
      h2.visually-hidden Список тем
      ul.themes
        li.themes__item
          a.themes__item-link(href='#')
            | Автомобили
            sup 88
        li.themes__item
          a.themes__item-link(href='#')
            | Удаленная работа
            sup 13
        li.themes__item
          a.themes__item-link(href='#')
            | Бизнес
            sup 13
        li.themes__item
          a.themes__item-link(href='#')
            | Путешествия
            sup 13
        li.themes__item
          a.themes__item-link.themes__item-link--padding(href='#')
            | Дизайн и обустройство
            sup 13
        li.themes__item
          a.themes__item-link(href='#')
            | Производство игрушек
            sup 22
        li.themes__item
          a.themes__item-link(href='#')
            | UX & UI
            sup 22
    .main-page__section-flex
      section.main-page__hot.hot
        h2.hot__name
          | Самое обсуждаемое
          span.hot__icon.hot__icon--fire
        ul.hot__list
          - for(var countArticles = 0; countArticles < 4; countArticles++)
            li.hot__list-item
              a.hot__list-link(href='#') #{apiArticlesData[countArticles].announce}
                sup.hot__link-sup #{countArticles}

      section.main-page__last.last
        h2.last__name
          | Последние комментарии
          span.last__icon.last__icon--cloud
        ul.last__list
          - var articleIndex = 4;
          - for(var countComment = 0; countComment < apiArticlesData[articleIndex].comments.length; countComment++)
            li.last__list-item
              img.last__list-image(src='/img/avatar-small-1.png' width='20' height='20' alt='Аватар пользователя')
              b.last__list-name Анна Артамонова
              a.last__list-link(href='#') #{apiArticlesData[articleIndex].comments[countComment].text}

    section.main-page__list.preview
      h2.visually-hidden Список превью статей
      ul.preview__list
        each apiArticle in apiArticlesData
          li.preview__item
            ul.preview__breadcrumbs
              each category in apiArticle.category
                li.preview__breadcrumbs-item
                  a.preview__breadcrumbs-link(href='#') #{category}
            .preview__background
              img.preview__background-image(src='/img/skyscraper@1x.jpg' width='460' height='240' srcset='/img/skyscraper@1x.jpg 1x, /img/skyscraper@2x.jpg 2x' alt='Фотография небоскреба')
            time.preview__time(datetime=apiArticle.createDate) #{apiArticle.createDate}
            h3.preview__name
              a.preview__name-link(href='#') #{apiArticle.title}
            p.preview__text #{apiArticle.announce}
            a.preview__comment(href='#')
              | Комментарии
              span.preview__cloud
              b.preview__comment-count #{apiArticle.comments.length}
